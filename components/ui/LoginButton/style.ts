import { createStyles } from '@mantine/styles'

const useStyles = createStyles((theme) => ({
  button: {
    color: '#f3f3f3',
    borderColor: '#f3f3f3',
  },
  buttonLogin: {
    color: '#072f37',
    borderColor: '#072f37',
  },
}))

export default useStyles
