import { createStyles } from '@mantine/styles'

const useStyles = createStyles((theme) => ({
  button: {
    color: '#E9ECEF',
    borderColor: '#E9ECEF',
  },
  buttonLogin: {
    color: '#072f37',
    borderColor: '#072f37',
  },
}))

export default useStyles
